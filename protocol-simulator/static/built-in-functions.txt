// LIST OF USABLE BUILT-IN FUNCTIONS & FIELDS

// function that is automatically called
// when a node receive a "msg"
receive(msg) {}

// function used to send a message
send(from, to, "type")
//usage: send(this.id, 2, "PING");

// function to get the amount of current actors
getActors()
//usage: this.approvalCount === getActors()

// function for creating a queue
createQueue()
//usage: this.messageQueue = createQueue();

// function for creating a timeout.
// A timeout makes a actor wait "ticks" before doing a "reaction"
timeout(actor, ticks, reaction)
//usage: timeout(this, 20, this.exitCS) - with exitCS being a function call

// field for color of a node
this.nodeColor = '#d8a01dff'
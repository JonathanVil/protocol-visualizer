/** @typedef {new (id: number) => { id: string }} ActorConstructor */

/** @typedef {{ source: number, destination: number, type: string}} Message */
// TODO: allow messages to carry a value


export {};